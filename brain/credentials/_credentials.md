# 🔐 Credentials — Index

> ⚠️ **SENSITIVE — would hold plaintext passwords/secrets if used.** Keep LOCAL. Never
> paste into external calls (web search / connectors), never sync to non-sanctioned
> services, never commit to a shared repo. **Best practice: use a real password manager
> and store only logins (no secrets) here.** Storing secrets in plaintext is a risk you
> accept knowingly.
>
> ⚙️ This folder starts EMPTY (no accounts). Add one file per client/context
> (`brain/credentials/<slug>.md`, copy `_template.md`) and list it in the table below.

## Conventions
- One file per client/context: `brain/credentials/<slug>.md`; this index lists them all.
- Always record: login, system, creation/rotation date, expiry if any.
- When deactivating an account, move its entry to the end of the file marked `⚪ inactive`.

## Accounts registered
| Client / context | System | Login | File |
|---|---|---|---|
| _(none yet)_ | | | |
