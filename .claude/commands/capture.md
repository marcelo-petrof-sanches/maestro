---
description: Quick-capture a note, task, decision, or client fact into the brain
---

Take what I give you and route it to the right place in the brain — no long response,
just file it and confirm in 1-2 lines.

**Retrieval-first:** file it where I'll *look for it*, not where it arrived. Ask "what
question will this answer later?" and put it there (see `brain/reference/brain-conventions.md`).

- A **task** → add to `brain/tasks/backlog.md` at the right priority (delegate to
  work-logger) AND create it in my Notion task database per `brain/tasks/notion-sync.md`.
- A **client fact / person / sensitivity** → delegate to client-keeper.
- A **durable decision** (methodology / scope / stakeholder / commercial) → log it via
  the `/decision-log` flow (next `D-NNN` in `brain/decisions/decision-log.md` + cross-post
  to the project file).
- A **load-bearing number** (one that'll be cited again) → the project's `## Current
  truth` block, with an as-of date + source — not loose in prose.
- A **progress note** → today's daily log + relevant project file.
- **Ambiguous?** Make a sensible call, note where you put it.

Capture this: $ARGUMENTS
