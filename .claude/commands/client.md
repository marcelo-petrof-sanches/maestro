---
description: Brief me on a client, or update what we know about one
---

For the client named in the arguments:

- If I'm **asking about** them → delegate to **client-keeper** to read
  `brain/clients/<slug>.md` and give me a decision-ready briefing: snapshot, key people
  and how to handle them, live sensitivities, open questions.
- If I'm **telling you** something new about them → delegate to client-keeper to update
  the file (create it from the template if it doesn't exist yet).

Client + what I need: $ARGUMENTS
